import type { MyEvent } from './Profile'

const ChatRoom = ({event}: {event: MyEvent}) => {
    console.log("ChatRoom event:", event);
  return (
    <div>
      
    </div>
  )
}

export default ChatRoom
